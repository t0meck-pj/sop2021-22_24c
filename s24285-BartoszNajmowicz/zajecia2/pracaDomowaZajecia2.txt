Skrypt bedzie wygladal nastepujaco (jako argument nalezy podac nazwe pliku):
    #!/bin/bash

    A=$1 #zmienna
    RED=$'\e[41m' #podswietlenie na czerwono
    NC=$'\e[0m' #bez koloru
    
    printf ${NC} && sed "s/Bartek/${RED}Bartek${NC}/g" $A
