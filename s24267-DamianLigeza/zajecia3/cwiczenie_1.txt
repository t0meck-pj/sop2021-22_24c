Podpunkt 1:
	#!/bin/bash

	if [[ $# < 1 ]]; then
		echo "Brak argumentow."
	else
		echo $1
	fi

Podpunkt 2:
	#!/bin/bash

	if [[ -e ./config.cfg ]]; then
		./config.cfg
		elif [[ $# == 1 ]]; then
			./$1
		else
			echo "Blad"
	fi
	
Podpunkt 3:
	